1. What is CSRF, XSS, SRI, CORS and how does it work in client side?

2. What is difference between virtual DOM and Shadow DOM ?

3. What is Temporal Dead Zone ?

4.What are Muttable  and Immutable elements in JS

    A mutable object is an object whose state can be modified after it is created.
    - objects and arrays are mutable, not primitive values.

    Immutables are the objects whose state cannot be changed once the object is created.
    - String and Numbers are Immutable.



